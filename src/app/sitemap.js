export default function sitemap() {
  const baseUrl = "https://rohan-codes-portfolio.vercel.app";

  return [
    {
      url: `${baseUrl}/`,
      lastModified: new Date(),
      changeFrequency: "weekly",
      priority: 1,
    },
  ];
}
